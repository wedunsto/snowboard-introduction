import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PracticeHeelStoppingPage } from './practice-heel-stopping.page';

describe('PracticeHeelStoppingPage', () => {
  let component: PracticeHeelStoppingPage;
  let fixture: ComponentFixture<PracticeHeelStoppingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PracticeHeelStoppingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
